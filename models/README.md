# Models Folder

This folder is used to store trained model artifacts generated by the notebooks.

Examples of possible saved model files include:

- Random Forest model
- Gradient Boosting model
- Autoencoder model
- Scaler objects
- Threshold values
